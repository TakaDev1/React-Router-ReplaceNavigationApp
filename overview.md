# 課題

## 問題

ログイン成功後にDashboardへ移動し、ブラウザの「戻る」を押してもLoginページに戻らないようにしてください。

## 条件

* `/login` にLoginページ
* `/dashboard` にDashboardページ
* Loginページに「ログイン」ボタン
* ボタンをクリックしたら`/dashboard`へ移動
* `replace: true`を使用する